# Copy to .env.local to override locally (optional — .env is committed with demo values).
NLITE_PUBLIC_APP_LABEL=nlite basic demo
NLITE_SERVER_SECRET=local-dev-secret
NLITE_ORIGIN=http://127.0.0.1:5173
